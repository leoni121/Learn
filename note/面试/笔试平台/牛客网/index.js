/*
while ((N=readInt()) != null && (M=readInt()) != null) {
  print (N + ' ' + M);
  let arr = [];
  for (let i=0; i<M; i++) {
    for(let j = 0;j<3;j++) {
      arr.push(readInt());
    }
    print(...arr);
    arr = [];
  }
}
while(line=readline()){
  var lines = line.split(' ');
  var a = parseInt(lines[0]);
  var b = parseInt(lines[1]);
  print(a+b);
}
*/
